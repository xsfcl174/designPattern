#设计模式:command(命令)
##属于:Behavioral(行为型)
##目的:将请求封装为对象，从而允许您使用不同的请求，队列或日志请求参数化客户端，并支持可撤销操作。
##适用范围
- 通过要执行的操作来参数化对象。
- 在不同时间指定，排队和执行请求。
- 支持撤消。
- 支持日志记录更改，以便在系统崩溃时可以重新应用它们。
- 构建围绕基本操作的高级操作系统。

##现实中的例子
- java.lang.Runnable
- Netflix Hystrix
- javax.swing.Action